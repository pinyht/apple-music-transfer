# Apple Music Transfer

Apple Music Transfer 是一个用于 Apple Music 数据备份与迁移的 Tampermonkey 用户脚本。

它主要用于：

- 备份 Apple Music 资料库中的专辑、歌曲、播放列表
- 在不同 Apple Music 账号之间迁移数据
- 在不同区服之间迁移数据
- 重建资料库与播放列表
- 在 Apple Music 订阅中断、资料库和播放列表丢失后进行恢复

## 当前支持

- 导出专辑
- 导出歌曲
- 导出播放列表
- 导入专辑
- 导入歌曲
- 导入播放列表

## 适用场景

- 更换 Apple ID 后迁移资料库
- 从一个区服迁移到另一个区服
- 先导出做本地备份，再按需重新导入
- 播放列表重建
- Apple Music 订阅到期后未续费，后续重新订阅时资料库或播放列表丢失
- 不想为了保留资料库而长期维持订阅，先做本地备份，后续再恢复

## 为什么需要备份

Apple Music 的资料库和播放列表并不适合被当作永久存档。

在实际使用里，下面这些情况都可能导致你需要重新恢复数据：

- 更换账号
- 切换区服
- 长时间停用订阅
- Apple Music 订阅到期后资料库与播放列表丢失

这个工具的一个重要价值就是：

- 先把专辑、歌曲、播放列表导出成可保存的本地文件
- 即使之后不持续订阅，也能保留一份自己的备份
- 后续重新订阅时，可以再把资料库和播放列表恢复回来

## 安装方式

需要：

- 已安装 Tampermonkey
- 可正常访问 `music.apple.com`
- 已登录 Apple Music 账号
- 如果需要跨区服迁移资料，需要具备对应区服可用的网络代理环境

安装后，在 `music.apple.com` 页面中启用脚本即可使用。

建议通过脚本库搜索 `Apple Music Transfer` 后直接安装。

补充说明：

- 在直连网络环境下，Apple Music 网页版通常会被强制落到中国大陆区
- 因此如果要切换到其他区服进行导出或导入，通常需要先准备对应区服可用的代理网络环境

## 使用说明

### 1. 导出专辑 / 导出歌曲

- 打开 Apple Music 网页版
- 打开脚本面板
- 选择 `导出`
- 进入对应导出项
- 点击开始导出

脚本会自动抓取数据并导出：

- `csv`
- `json`

导出的 `json` 可用于后续导入。

### 2. 导出播放列表

- 进入某个播放列表详情页
- 打开脚本面板
- 选择 `导出 -> 导出播放列表`
- 点击开始导出

播放列表导出会保留：

- 播放列表名称
- 播放列表简介
- 歌曲顺序
- 歌曲 ID / 歌曲名 / 歌手名 / 专辑名

### 3. 导入专辑 / 导入歌曲 / 导入播放列表

- 打开脚本面板
- 选择 `导入`
- 进入对应导入项
- 点击 `导入 JSON`
- 再点击 `开始导入`

说明：

- 导入专辑、歌曲时，脚本会根据 ID 打开详情页并自动判断是否可加入资料库
- 导入播放列表时，脚本会根据歌曲 ID 逐条加入目标播放列表
- 如果目标播放列表不存在，脚本会自动新建

## 支持的显示语言

当前脚本支持以下显示语言切换：

- 简体中文
- 繁体中文（香港）
- 繁体中文（台湾）
- 英文
- 日文
- 韩文

其中主要导入导出流程目前已重点验证：

- 简体中文
- 繁体中文（台湾）
- 英文
- 韩文

其余语言可继续根据实际页面文案补充适配。

## 支持的区服

当前脚本内置以下常用区服切换：

- 美国区（us）
- 中国大陆区（cn）
- 日本区（jp）
- 中国香港区（hk）
- 中国台湾区（tw）
- 新加坡区（sg）
- 英国区（gb）
- 韩国区（kr）
- 加拿大区（ca）
- 澳大利亚区（au）
- 法国区（fr）
- 德国区（de）

说明：

- 脚本支持在这些区服之间执行导出、导入和迁移
- 但不同区服的版权与上架情况不同，部分歌曲、专辑或播放列表歌曲可能无法恢复
- 区服切换是否成功，还取决于当前网络环境是否支持目标区服

## 数据说明

### 专辑导出

主要包含：

- 专辑 ID
- 专辑名
- 歌手名

### 歌曲导出

主要包含：

- 歌曲 ID
- 歌曲名
- 歌手名
- 专辑名

### 播放列表导出

主要包含：

- 播放列表名
- 播放列表简介
- 歌曲 ID
- 歌曲名
- 歌手名
- 专辑名

## 注意事项

- 不同区服的版权与上架情况不同，部分歌曲或专辑可能无法导入
- 导入条目很多时，执行时间会比较长
- 播放列表导入依赖网页端菜单交互，执行期间不要手动干扰页面
- 建议先导出 `json` 做备份，再执行导入
- 即使是同一个账号、同一个区服，重新订阅后也可能出现资料库或播放列表丢失，因此建议定期备份
- 如果没有代理网络环境，跨区服切换可能失败，Apple Music 也可能自动回到中国大陆区

## 更新日志

### v1.0 (2026-03-09)

- 首个公开版本
- 支持导出专辑、歌曲、播放列表
- 支持导入专辑、歌曲、播放列表
- 支持跨账号、跨区服迁移
- 支持在订阅中断后恢复资料库与播放列表

## 许可证

MIT
